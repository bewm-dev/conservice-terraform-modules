# =============================================================================
# Production Environment Overrides
#
# Values here override infra.yaml for the prod environment only.
# Only include sections you need to override — everything else
# uses the defaults from infra.yaml.
# =============================================================================

# Example: restrict prod admin to a specific group
# databases:
#   my_app:
#     admin_groups: [aws-db-billing-admin]
#     admin_users: [shawnpetersen]             # break-glass individual access
